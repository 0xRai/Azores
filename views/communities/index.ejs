<% layout('layouts/boilerplate') %>
    <h1>All Communities</h1>
    <ul>
        <% for (let community of communities) { %>
            <div class="card mb-3" mb-3>
                <div class="row">
                    <div class="col-md-8">
                        <div class="card-body">
                            <h5 class="card-title">
                                <%= community.name %>
                            </h5>
                            <a href="/c/<%= community._id%>" class="form-button btn-primary">View <%= community.name %></a>
                        </div>
                    </div>
                </div>
            </div>
            <% } %>
    </ul>
    <script>
        const mapToken = '<%-process.env.MAPBOX_TOKEN%>';
        const communities = {
            /* features: <%- JSON.stringify(communities) %> */
        }
    </script>
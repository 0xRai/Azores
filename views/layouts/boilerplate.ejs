<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="shortcut icon" href="/images/azores.svg" type="image/x-icon">
    <link rel="stylesheet" href="/stylesheets/build/reset.css">
    <link rel="stylesheet" href="/stylesheets/build/style.css">
    <title>
        <% try { %>
            <%- title %> |
                <% } catch { %>
                    <% } %>
                        Azores
    </title>
</head>

<body>
    <%- include('../partials/navbar') %>
        <% if (!currentUser) {%>
            <%- include('../partials/modals/auth.ejs') %>
                <% } %>
                    <% if ( error !='' ) { %>
                        <div class="flash-alert alert-warning" id="alert">
                            <p class="flash-alert-message">
                                <%= error %>
                            </p>
                        </div>
                        <% } %>
                            <% if ( success !='' ) { %>
                                <div class="flash-alert alert-success" id="alert">
                                    <p class="flash-alert-message">
                                        <%= success %>
                                    </p>
                                </div>
                                <% } %>
                                    <%- body %>

</body>
<script src="/javascripts/alerts.js"></script>
<script src="/javascripts/searchBar.js"></script>
<script src="/javascripts/modal.js"></script>

</html>